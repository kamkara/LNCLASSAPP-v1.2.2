<%= form_with(model: citytown, local: true) do |form| %>
  <% if citytown.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(citytown.errors.count, "error") %> prohibited this citytown from being saved:</h2>

      <ul>
        <% citytown.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field my-2">
    <%= form.text_field :title, placeholder:"Nom de la ville ..." %>
  </div>

  <div class="actions mt-2">
    <%= form.submit "Add new city", class:'inline-flex justify-center py-2 px-4 border border-transparent 
  shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600
 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2
  focus:ring-indigo-500' %>
  </div>
<% end %>
